/**
 * HitboxManager.js — Per-frame sphere overlap tests for combat.
 * Phase 3 implementation — stub for now.
 */

export class HitboxManager {
    constructor() {
        this.hitboxes = [];
    }

    /**
     * Resolve all active hitbox overlaps.
     */
    resolveOverlaps() {
        // Phase 3: sphere-sphere overlap checks
    }
}
