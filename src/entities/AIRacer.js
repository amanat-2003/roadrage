/**
 * AIRacer.js — Single AI opponent with FSM behaviour.
 * Phase 2 implementation — stub for now.
 */

export class AIRacer {
    constructor(centreline, trackMesh, personality) {
        this.centreline = centreline;
        this.trackMesh = trackMesh;
        this.personality = personality;
        // Phase 2: full FSM implementation
    }

    fixedUpdate(dt) {
        // Phase 2
    }
}
